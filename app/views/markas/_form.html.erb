<%= form_with(model: marka) do |form| %>
  <% if marka.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(marka.errors.count, "error") %> prohibited this marka from being saved:</h2>

      <ul>
        <% marka.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Marka, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit 'Kaydet' %>
  </div>
<% end %>
